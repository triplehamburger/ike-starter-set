<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>network.ike.foundation</groupId>
        <artifactId>ike-starter-set</artifactId>
        <version>1-SNAPSHOT</version>
    </parent>

    <artifactId>ike-hierarchy-maven-plugin</artifactId>
    <packaging>maven-plugin</packaging>

    <name>Ike Hierarchy Maven Plugin</name>
    <description>
        Maven 4 plugin for the decentralized live hierarchy. Registers an AsciiDoc file as a chapter
        in one command without moving it or editing the guide, discovers every chapter declared
        anywhere in the reactor, validates the hierarchy they form, and writes the index that
        document rendering reads.
    </description>

    <!-- Dual licensed (IKE-Network/ike-issues#952); identical in every POM of this reactor. -->
    <licenses>
        <license>
            <name>Creative Commons Attribution 4.0 International</name>
            <url>https://creativecommons.org/licenses/by/4.0/</url>
            <distribution>repo</distribution>
        </license>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <properties>
        <maven.compiler.release>25</maven.compiler.release>

        <!-- These two versions are the first thing to check if this module fails to build.
             The Maven 4 plugin API (org.apache.maven.api.*) is annotated @Experimental and is not
             guaranteed compatible between release candidates, so maven.api.version must match the
             Maven 4 runtime this plugin will actually run under. Maven 4.0.0 GA had not shipped as
             of this writing; 4.0.0-rc-6 is the newest release candidate.
             maven-plugin-tools must be a 4.x version: only those understand the v4 annotations in
             org.apache.maven.api.plugin.annotations. -->
        <maven.api.version>4.0.0-rc-5</maven.api.version>
        <maven-plugin-tools.version>4.0.0-beta-2</maven-plugin-tools.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>network.ike.foundation</groupId>
            <artifactId>ike-hierarchy-model</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.apache.maven</groupId>
            <artifactId>maven-api-core</artifactId>
            <version>${maven.api.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.maven</groupId>
            <artifactId>maven-api-di</artifactId>
            <version>${maven.api.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.maven.plugin-tools</groupId>
            <artifactId>maven-plugin-annotations</artifactId>
            <version>${maven-plugin-tools.version}</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>3.27.3</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <release>25</release>
                    <compilerArgs combine.self="override"/>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <argLine combine.self="override"/>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-plugin-plugin</artifactId>
                <version>${maven-plugin-tools.version}</version>
                <configuration>
                    <!-- Resolves 'mvn ike-hierarchy:<goal>'. Set explicitly rather than derived,
                         because the derived prefix depends on plugin-tools' naming rules and this
                         string appears in documentation, scripts, and muscle memory. -->
                    <goalPrefix>ike-hierarchy</goalPrefix>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
