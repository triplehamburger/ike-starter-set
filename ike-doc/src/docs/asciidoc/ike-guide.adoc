= Ike Starter Set
:toc: left
:sectnums:

[preface]
== How to Read This Guide

=== The Identicon

Every component in the knowledge base has a formal public identity. This guide makes
that identity visible as an *identicon*: a small square image computed
deterministically from the component's own public identifier — a visual hash of
identity, and nothing else. Its purpose is recognition: the same component draws
exactly the same identicon everywhere it appears — in this guide's HTML and PDF, in
print, and onscreen in applications such as Komet, all of which compute it from the
same identity data — so you can recognize a component at a glance, and see that two
references name the same thing, before reading a word. Because it is derived from
identity alone, an identicon carries no meaning about what the component is; the
name and the sigil carry that.

.The identicon of English Language, computed from its public identifier — the same image wherever this component appears
koncept-identicon-figure::EnglishLanguage[width=96]

=== Koncept Badges

Every formally identified term in this guide renders as a *Koncept Badge*: a kind
sigil when the kind calls for one (next section), then the identicon, then the
component's name in small caps. A sigil always immediately precedes an identicon —
it marks what kind of thing the identicon identifies, and never stands alone in a
badge. A badge is not styling; it is a reference to one exact component in the
knowledge base, and it links to that koncept's entry in the Koncept Glossary at
the end of this book, where its definition, axioms, and identifiers live. The prose is knowledge-base-native too:
each chapter's narrative is itself stored in the knowledge base and rendered here,
so the same content backs this guide and every other surface that reads the set.

.Anatomy of a Koncept Badge — Description Pattern's badge, dissected
koncept-badge-anatomy::DescriptionPattern[]

A STAMP badge follows the same anatomy with provenance in place of a name — shown
here for this set's own inception STAMP, declared in the ledger source as
Active · Inception · IKE Community · IkeFoundation module · Development path.
Inception is the platform's own named instant for time before any release — every
version in this pre-release set carries it, and it renders as the word rather
than a date on every surface.

// The set's ONE declared inception stamp (Ike.INCEPTION: Stamp.active(PrimitiveData.INCEPTION_EPOCH,
// IKE Community, IkeFoundation module, Development path)). Stamp identity is derived from the
// tuple itself — Stamp.stampUuid = T5(STAMP_NAMESPACE, status|time|author|module|path) — so this
// UUID is the stamp's real, replay-stable PublicId, computed from the declared constituents;
// FoundationFidelityIT's stamp-pair gate pins it (IKE-Network/ike-issues#894).
.Anatomy of a STAMP badge — the set's inception stamp, dissected
koncept-badge-anatomy::uuid=45b13d09-7c50-5495-8460-aef66a2ae615[kind=stamp, label="Active · Inception · IKE Community"]

=== The Component Kinds

Everything in the knowledge base is one of a small set of component kinds, and a
badge is honest about which kind it names. The scheme is _Koncept bare, everything
else marked_: a Koncept badge carries no extra mark, and every other kind announces
itself with a sigil ahead of the identicon.

.The component kinds, their badge sigils, and a concrete example of each
[cols="1,2,4",options="header"]
|===
| Kind | Badge | What it is

| Koncept
| no sigil — bare +
e.g. k:EnglishLanguage[]
| A *Koncept* — with a K — is the formal construct in the knowledge base,
deliberately distinct from a concept in the mind. A concept is the thought a
person holds; a Koncept is the identified, versioned construct that carries
descriptions (its names) and a logical definition (its meaning) — formally, the
referent of the semiotic triangle: the thing the badge's name symbolizes and a
reader's concept refers to. Most badges in this guide are Koncepts.

| Pattern
| koncept-sigil:pattern[scale=1.5] violet P +
e.g. k:DescriptionPattern[]
| The schema for a family of semantics: what a semantic of this pattern means as a
whole, and the ordered fields it carries — each with its own meaning, purpose, and
data type. Every pattern in this set has its own shape table (see below).

| Description
| koncept-sigil:description[scale=1.5] amber D +
e.g. koncept-sigil:description[Uninitialized Component (SOLOR)]
| Human-readable text bound to a concept — itself a semantic, of
k:DescriptionPattern[] or a dialect pattern. Every name rendered in this guide,
including the text inside each badge, is a description in the knowledge base.

| Semantic
| koncept-sigil:semantic[scale=1.5] green S +
e.g. koncept-sigil:semantic[Gretel]
| A structured assertion attached to any component, instantiating exactly one
pattern: its fields hold the values, in the pattern's declared order. The _e.g._
rows in the shape tables are read from real semantics.

| STAMP
| koncept-sigil:stamp[scale=1.5] grey pentagon +
e.g. koncept-sigil:stamp[Active · Inception · Tinkar Starter Data Author]
| The provenance every version of every component carries: status, time, author,
module, and path. A STAMP badge leads with the pentagon sigil — a point-up
five-dimensional radar, one reading per STAMP dimension — then the STAMP's own
identicon, which tells one STAMP from another at a glance, then the compact
provenance text (status, time, author) in place of a name: a STAMP is identified
like every component, but what it says is provenance.
|===

The concept and pattern examples are live badges — click either to visit its
glossary entry. The description, semantic, and STAMP examples are _specimen_
badges drawn from the set's real content — its earliest-authored description, the
author roster's first entry, and the set's inception provenance — shown unlinked
and without identicons because those components are not named terms in this
guide's glossary. On live surfaces that render individual semantics and versions
(Komet itself, chat and mail integrations) the D, S, and pentagon badges appear
in full, identicons included; the badge language is the same across all of
them. A red question-mark sigil (koncept-sigil:unknown[]) marks a reference whose
kind could not be determined.

=== Pattern-Shape Tables

Each pattern's chapter renders its shape as a numbered table — for example
<<koncept-pattern-table-DescriptionPattern>> — organized as one three-row group per
*Model Feature*: first the pattern's referenced component, then each declared
field, 1-indexed, in declared order.

* An italic label row names the feature: _Referenced component_ (the component —
  concept, semantic, pattern, or STAMP — this pattern's semantics attach to) or
  _Field N_. The referenced component is never numbered "Field 0" because it is
  not part of the fields array: it is a chronicle-level property of the semantic,
  structurally separate from the field values.
* A content row names the feature's *Meaning* — the concept saying what a value of
  this feature is — its *Purpose* — the concept saying why the pattern carries
  it — and its *Data type*, drawn from the closed set of data-type concepts the
  platform actually resolves. The referenced component has no data type, so that
  cell is an em dash.
* An indented _e.g._ row shows the feature's value on one real semantic already in
  the knowledge base — deterministically the earliest-authored one, so the example
  is stable across regenerations — rendered as a badge when the value is itself an
  identified component, and as plain text otherwise. A pattern with no semantics
  yet simply has no e.g. rows.
* An indented _default_ row shows the field's value from the pattern's default
  value semantic (see the Default Values chapter), rendered the same way. Only a
  pattern with an authored default carries these rows.

=== The Glossary and the List of Patterns

Every koncept referenced anywhere in this guide has an entry in the comprehensive,
alphabetical Koncept Glossary that closes the book — definition, axioms,
identifiers, and taxonomy links to parents and children. The List of Patterns
closing the first chapter indexes every pattern's glossary entry, the same book
convention as a List of Tables or a List of Figures. Occasional indented concept
trees show a live taxonomy subtree of the knowledge base, each node a badge.

== What This Set Provides

The IkeFoundation knowledge set declares its content as an append-only ledger, rooted
at k:IkeFoundationRoot[]; this guide and its comprehensive glossary are generated from
the same materialized store as the released change set.

The chapters that follow describe how this set's own concepts support the platform's
core mechanisms — language and dialect, STAMP versioning, logical and navigational
coordinates, EL++, the field-level meta-schema, and the base component/chronicle
model itself — each naming the actual koncepts involved. The Koncept Glossary at the
end lists every koncept in the set, alphabetically, for lookup.

Every pattern in the set is defined by a referenced-component meaning and purpose,
plus a declared, ordered list of fields, each with its own meaning, purpose, and data
type — a pattern's own semantics are exactly the set of components carrying that
pattern's public id. The list below indexes all of them — a List of Patterns, the same book
convention as a List of Tables or a List of Figures, styled like the table of contents
above — each entry linking to that pattern's own full shape, rendered as a table in the
Koncept Glossary.

koncept-pattern-list::[]

== Language Concepts

koncept-narrative::DescriptionPattern[]

<<koncept-pattern-table-DescriptionPattern>> shows the pattern's full shape: each
Model Feature — the referenced component, then each field in declared order — with
its meaning, purpose, and data type, and a live example value drawn from a real
semantic already in the set.

koncept-pattern-table::DescriptionPattern[]

The dialect patterns that layer acceptability on top of a description are shown in
<<koncept-pattern-table-USDialectPattern>> and
<<koncept-pattern-table-GBDialectPattern>>.

koncept-pattern-table::USDialectPattern[]

koncept-pattern-table::GBDialectPattern[]

== STAMP Concepts

Every version of every component carries a STAMP: a (status, author, module, path,
time) tuple recording who committed it, in what state, in which module, on which
path, and when. Four of those five dimensions are themselves concepts in this set.

=== Status

koncept-narrative::StatusValue[]

=== Author

koncept-narrative::Author[]

=== Module

koncept-narrative::Module[]

<<koncept-pattern-table-ModuleOriginsPattern>> shows the module-origins pattern's
shape.

koncept-pattern-table::ModuleOriginsPattern[]

=== Path

koncept-narrative::Path[]

<<koncept-pattern-table-PathOriginsPattern>> and
<<koncept-pattern-table-VersionControlPathPattern>> show the two path patterns'
shapes.

koncept-pattern-table::PathOriginsPattern[]

koncept-pattern-table::VersionControlPathPattern[]

=== Edit Coordinate

koncept-narrative::AuthorForEditCoordinate[]

Put together, a `StampCoordinate` is exactly these four dimensions plus a time: the
allowed states, the module preference and exclusion, and a position (path plus time).
`StampCalculator` walks a component's version history and returns whichever version
is both on an allowed path (at or before the coordinate's time) and in an allowed
state — "latest state," computed the same way every time the same coordinate is used.

As a pattern, k:STAMPPattern[] itself carries a referenced-component meaning of
k:VersionProperties[] and a purpose of k:CommitProvenance[] — why the pattern exists:
to record who committed a version, in what state, module, path, and when. Five fields
each carry their own meaning and purpose concept: k:StatusValue[] (meaning) for
k:StatusForVersion[] (purpose), k:Time[] (meaning) for k:TimeForVersion[] (purpose),
and k:Author[], k:Module[], and k:Path[] (meaning) for k:AuthorForVersion[],
k:ModuleForVersion[], and k:PathForVersion[] (purpose) respectively — the same four
koncepts named above, plus time, now restated here by their own exact field-meaning
identity. That shape, feature by feature, is <<koncept-pattern-table-STAMPPattern>>.

koncept-pattern-table::STAMPPattern[]

== Logic Coordinates

koncept-narrative::LogicCoordinateProperties[]

<<koncept-pattern-table-SolorConceptsPattern>> and
<<koncept-pattern-table-SOLORConceptAssemblage>> show the membership patterns named
here — the fresh Solor Concepts Pattern and the dormant legacy SOLOR concept
assemblage it supersedes.

koncept-pattern-table::SolorConceptsPattern[]

koncept-pattern-table::SOLORConceptAssemblage[]

== Navigation Coordinates

koncept-narrative::NavigationVertex[]

<<koncept-pattern-table-StatedNavigationPattern>> and
<<koncept-pattern-table-InferredNavigationPattern>> show the two navigation
patterns' shapes.

koncept-pattern-table::StatedNavigationPattern[]

koncept-pattern-table::InferredNavigationPattern[]

== EL++ Concepts

koncept-narrative::Axioms[]

=== Roles and Role Operators

koncept-narrative::Role[]

=== Inclusion, Necessary, and Sufficient Sets

koncept-narrative::InclusionSet[]

=== Concrete Value Operators

koncept-narrative::ConcreteValueOperator[]

=== Logical Definitions and Grouping

koncept-narrative::LogicalDefinition[]

<<koncept-pattern-table-ELStatedAxiomsPattern>>,
<<koncept-pattern-table-ELInferredAxiomsPattern>>, and
<<koncept-pattern-table-OWLAxiomSyntaxPattern>> show the three axiom patterns'
shapes.

koncept-pattern-table::ELStatedAxiomsPattern[]

koncept-pattern-table::ELInferredAxiomsPattern[]

koncept-pattern-table::OWLAxiomSyntaxPattern[]

== Semantic Field Model

This chapter describes the field-level meta-schema: what kind of value a pattern's
field holds, how that value renders in Komet's UI, what its value can mean, and how
a field's legal values can be constrained.

=== Field Categories

koncept-narrative::FieldCategories[]

=== Display Fields

koncept-narrative::DisplayFields[]

=== Field Value Terminology

koncept-narrative::Meaning[]

=== Constraining Fields

koncept-narrative::ConstrainedPattern[]

[koncept-tree]
----
kindof: Author
----

This section is the shape-level introduction. The Field Constraints chapter, next,
describes how the starter set supports constraints in practice — authoring and
retrieval, conjunctive composition, each shape's legal-set derivation, the member
match relations, and coherence with default values — and renders the two constraint
patterns' shapes as <<koncept-pattern-table-TaxonomyFieldConstraintPattern>> and
<<koncept-pattern-table-ValueSetFieldConstraintPattern>>.

== Field Constraints

Where the Semantic Field Model chapter's Constraining Fields section introduces the
constraint apparatus — two patterns, one per parameter shape, both attaching to the
pattern whose field they constrain — this chapter describes how the starter set
supports constraints in practice: what authoring one looks like, how several
constraints on one field compose, how each shape derives its legal set, and how
constraints and default values cohere.

A constraint is first-class content of the pattern it constrains. Unlike a default
value — which needs the Defaults and templates module as its category boundary (see
the Default Values chapter) — a constraint needs no machinery at all: no special
module, no exclusion from any read path, no dedicated accessor. Each constraint
semantic attaches to the constrained pattern as its referenced component, and reading
a pattern's constraints is retrieval by enumeration: walk the semantics attached to
the pattern that are of the two constraint patterns. Identity is declared
descriptively, so the ledger reads as prose — the STAMP author constraint below is
declared as "Taxonomy Field Constraint: STAMP pattern Author field kind-of Author",
the rule's own name saying exactly what it asserts.

Constraint semantics compose conjunctively. Each semantic is one conjunct, and a value
is legal only when every conjunct on its field holds — no override, no precedence, no
disjunction to reason about. A field may carry constraints of both shapes at once, a
taxonomy constraint and a value-set constraint intersecting: legal then means inside
the taxonomy region *and* matching an enumerated member.

The pattern axis is the parameter shape: one constraint pattern per parameter tuple,
never one per datatype, and never a union carried by sentinel values. A kind or
relation field earns its place as a genuine parameter only when every field of the
tuple stays meaningful for every one of its values — the four taxonomy kinds share one
identical (field, kind, anchor) tuple, so one pattern carries all four; value-set
membership carries a different tuple entirely, so it is a pattern of its own — a
shape, never a fifth kind. Every field of every constraint semantic is meaningful: no
k:BlankConcept[] sentinel anywhere in this story.

=== The Taxonomy Field Constraint

koncept-narrative::TaxonomyFieldConstraintPattern[]

<<koncept-pattern-table-TaxonomyFieldConstraintPattern>> shows the pattern's shape.

koncept-pattern-table::TaxonomyFieldConstraintPattern[]

The starter set carries five taxonomy constraints on real, long-standing patterns —
the four STAMP dimensions and the Description Pattern's language field
(<<koncept-pattern-table-STAMPPattern>> and <<koncept-pattern-table-DescriptionPattern>>
show the constrained patterns' own shapes):

* k:STAMPPattern[]'s author, module, and path fields are each kind-of constrained to
  their own dimension's taxonomy — anchors k:Author[], k:Module[], and k:Path[] — the
  anchor itself a legal value, and every new author, module, or path minted beneath it
  a legal value from the moment it exists.
* k:STAMPPattern[]'s status field is immediate-child constrained to k:StatusValue[]:
  STAMP status is a closed, one-level value list — exactly k:ActiveState[],
  k:InactiveState[], k:PrimordialState[], k:CanceledState[], and k:WithdrawnState[] —
  and the constraint says so as data.
* k:DescriptionPattern[]'s language field (meaning
  k:LanguageConceptNidForDescription[]) is kind-of constrained to k:Language[]: any
  language concept, at any depth, is a legal description language.

=== The Value-set Field Constraint

koncept-narrative::ValueSetFieldConstraintPattern[]

<<koncept-pattern-table-ValueSetFieldConstraintPattern>> shows the pattern's shape.

koncept-pattern-table::ValueSetFieldConstraintPattern[]

The set's worked value-set constraint is the one the Default Values chapter's worked
default already satisfies: k:PreferredReviewerPattern[]'s single field,
k:PreferredReviewer[], is constrained to membership in the starter set author roster.
The constraint's four fields read: constrained field = k:PreferredReviewer[],
value-set pattern = k:StarterSetAuthorRosterPattern[], value-set field =
k:RosterAuthor[], member match relation = k:EqualMatchRelation[]
(<<koncept-pattern-table-PreferredReviewerPattern>> and
<<koncept-pattern-table-StarterSetAuthorRosterPattern>> show both patterns' shapes).

The roster is why the k:ValueSetField[] pointer exists. Each roster entry is a
semantic with two fields — k:RosterAuthor[], the member, and k:RosterOrder[], a
display order — and only the first is what a candidate value is matched against:
k:RosterOrder[] is a characteristic of the entry, not the member. The pointer names
the member-bearing Model Feature by its meaning concept — a declared field's meaning,
or, for a membership pattern whose members are the referenced components themselves,
the source pattern's referenced-component meaning — so any pattern whose active
semantics enumerate things can serve as a value set, with no value-set-specific
machinery on the source side.

=== The Member Match Relation

koncept-narrative::MemberMatchRelation[]

One relation is admitted today:

koncept-narrative::EqualMatchRelation[]

=== The Apparatus Constrains Itself

The apparatus's own discriminator fields are exactly the closed one-level value lists
the immediate-child kind exists for, and the set constrains them accordingly — as
ordinary content, through the same pattern any other constraint uses:

* "Taxonomy Field Constraint: Taxonomy Field Constraint Pattern Constraint kind field
  immediate-child of Taxonomy field constraint kind" — constrained field =
  k:ConstraintKind[], kind = k:ImmediateChildFieldConstraint[], anchor =
  k:TaxonomyFieldConstraintKind[]: the kind field of every taxonomy constraint holds
  one of the four kinds, and nothing else.
* "Taxonomy Field Constraint: Value-set Field Constraint Pattern Member match relation
  field immediate-child of Member match relation" — constrained field =
  k:MemberMatchRelation[], kind = k:ImmediateChildFieldConstraint[], anchor =
  k:MemberMatchRelation[] again: the relation field of every value-set constraint
  holds an admitted relation, and nothing else — the same concept serving as the
  field's meaning and the constraint's anchor without conflict, the two roles read
  from different fields entirely.

Nothing marks these two semantics as special: they are taxonomy constraints like the
STAMP pattern's four, retrieved the same way, composed the same way. Growing either
closed list is authoring: mint the child concept, and the existing constraint admits
it without being touched — for a relation, the bijection gate additionally requires
its evaluator to ship in the same change.

=== Constraints and Defaults

Constraints and defaults meet at one coherence rule: a default value must satisfy its
field's constraints — a fresh semantic pre-populated from defaults should be legal
where a curator has finished choosing, and loudly illegal where they have not.

k:PreferredReviewerPattern[] shows the finished side: its authored default,
k:Gretel[], is the roster's own first entry, so the default satisfies the value-set
constraint on the very field it fills — a real, constraint-satisfying value, visible
as the _default_ row of <<koncept-pattern-table-PreferredReviewerPattern>> (the
Default Values chapter walks the full round trip).

The loud defaults show the unfinished side, deliberately. k:UninitializedComponent[]
belongs to no domain taxonomy region and no curated value set, and `NaN` matches
nothing under k:EqualMatchRelation[], itself included. So wherever a constrained field
still carries its loud default, constraint checking fails it until someone chooses a
real value — the same machinery that validates finished data announces unfinished
data, the loud-defaults principle of the Default Values chapter restated from the
checking side.

== Default Values

An editor opening a fresh semantic needs a starting value for every field — the store
rejects null field values outright. This chapter describes how the starter set supports
default values with no defaults-specific machinery in the model at all: a default is
ordinary working-path content — a complete semantic of the target pattern — kept out of
domain reads by two structural dimensions the model already has, its referenced
component and its module.

=== The Default Value Semantic

koncept-narrative::DefaultValueConcept[]

=== The Defaults and Templates Module

koncept-narrative::DefaultsAndTemplatesModule[]

=== Templates

koncept-narrative::TemplateConcept[]

=== Loud Defaults for Every Data Type

What value should a default actually carry? The governing principle is *loud defaults*:
a default must work as an initial value while reading as obviously needing revision —
never Java's silent zero-values, which disappear into real data instead of announcing
themselves. The starter set demonstrates the principle at full breadth with one
pattern carrying a loud default for every data type a pattern field can declare.

koncept-narrative::DataTypeDefaultsPattern[]

<<koncept-pattern-table-DataTypeDefaultsPattern>> shows the pattern's full shape; the
indented _default_ row under each field is read live from the pattern's own default
value semantic, through the same computed identity any editor would resolve.

koncept-pattern-table::DataTypeDefaultsPattern[]

The sixteen decided defaults group into a small number of strategies:

* *The UNINITIALIZED string forms.* String is the literal `UNINITIALIZED`; ByteArray is
  the UTF-8 bytes of `UNINITIALIZED` — the loud String default carried in byte form;
  Array is an array of exactly one element, the String `UNINITIALIZED` — present and
  non-empty, since an empty array would be a silent zero-value.
* *The loud placeholder component.* Component and Concept default to
  k:UninitializedComponent[], the base set's own loud placeholder for a value still
  owed — deliberately not k:BlankConcept[]: Blank means a deliberate not-applicable, a
  quiet, legitimate state, while Uninitialized means a value still owed. ComponentIdSet
  and ComponentIdList are the singleton set and list holding k:UninitializedComponent[]
  — present and non-empty, their one member itself the loud placeholder.
* *Native non-values.* Types whose value space offers a true non-value use it — a
  non-value is louder still, because it cannot be mistaken for data at all. Float is
  `NaN`, which also propagates through arithmetic, so any computation over an unrevised
  default stays loudly not-a-number; Instant is the premundane instant, the time type's
  own before-all-recorded-time marker, chosen over a sentinel date because a true
  non-value cannot be mistaken for a real timestamp.
* *Stretched-sevens sentinels.* Numeric types without a native non-value use a planted
  repdigit: Integer is `777777777` (nine sevens), Long is `777777777777777777`
  (eighteen sevens), and Decimal is `777777777.777` — nine sevens, a decimal point,
  three more sevens, the decimal point placed deliberately to demonstrate the type's
  distinguishing property: a scaled decimal, not an integer. A repdigit signature reads
  as deliberately planted and cannot plausibly occur as natural data, where `MIN_VALUE`
  would read as an overflow bug.
* *Structural values that prove their type.* DiGraph is a deliberate simple cycle — two
  vertices, both k:UninitializedComponent[], with edges in both directions: a cycle can
  never be a tree, so the value proves the field truly holds a graph and not a tree.
  DiTree is the smallest well-formed tree — a single vertex, the loud placeholder at
  its root.
* *A real, resolvable semantic.* The Semantic field's default is the fully qualified
  name description semantic of k:UninitializedComponent[] — a real, resolvable semantic
  whose subject is itself the loud placeholder, so the reference is valid yet plainly
  unrevised.
* *The flagged compromise.* Boolean is `false`: a two-valued type has no loud member,
  so no Boolean default can announce itself as unrevised; `false` is offered as the
  initial value with that limitation stated rather than hidden.

=== A Worked Default: Preferred Reviewer

k:PreferredReviewerPattern[] carries the set's other default value semantic — the
minimal end-to-end demonstration the sixteen-type exemplar generalizes. Its single
field, k:PreferredReviewer[], is value-set constrained to the
k:StarterSetAuthorRosterPattern[] roster (see the Field Constraints chapter), and
its authored default is k:Gretel[], that roster's first entry — a real,
constraint-satisfying default value, not a placeholder. The full round trip is visible
in this guide itself: the default is authored as ordinary ledger content in the
Defaults and templates module, `StampCalculator.getDefault(pattern)` resolves it by
computed identity, and the same value renders as the _default_ row of
<<koncept-pattern-table-PreferredReviewerPattern>>.

== Clinical Quality Language (CQL) Queries

CQL (Clinical Quality Language) is the HL7 (Health Level 7) standard expression language used to author clinical quality measures and decision-support logic -- the language behind retrieve expressions, timing phrases, and query clauses that pull structured data from a FHIR (Fast Healthcare Interoperability Resources) source and evaluate it against a measure's logic. This chapter inventories every reserved keyword and lexical construct in the language and tracks how each one is being represented as a concept inside Komet, the IKE-Foundation terminology authoring platform.

Keywords are grouped into *five families -- Types & Literals, Declarations, Core Operators, Data & Timing Operators, and Query & Control Flow* -- each a top-level section below, with its categories as subsections.

Every keyword entry carries a *category* (its grammatical role -- declaration, operator, type, and so on), a plain-language *definition* drawn from the CQL specification, a worked *example* showing it in realistic use, and a *Komet status* flag showing whether it already exists as an authored concept, has a related candidate concept, or has no match yet.

Each entry in the <<keyword-dictionary,Keyword Dictionary>> states, in order:

* Its *category* (e.g. `Timing Operator`) within its family
* A plain-language *definition*
* A worked *example* -- illustrative CQL, written to show the keyword in a realistic clinical-quality-measure context, not copied from any single real measure



=== Running CQL Queries

Running a CQL Query info here

=== Family Summaries

Quick-scan view of each family in this guide and what it contributes to CQL authoring.

==== Types & Literals (8)

This family defines the foundational values CQL expressions operate on: built-in types (`Code`, `Concept`, `Interval`, `List`, `Tuple`) and fixed literals (`true`, `false`, `null`). These terms establish the basic data shapes and constants used throughout the language.

==== Declarations (19)

This family contains the top-level library-building language: context, terminology, parameters, includes, and reusable definitions, plus modifiers for visibility, aliasing, defaults, fluent use, display labels, and versions. In practice, these keywords structure a CQL library and control how its logic is exposed and reused.

==== Core Operators (18)

This family covers general-purpose expression logic: boolean composition, arithmetic operations, type checks/conversions, and core comparison (`between`). These terms are the primary tools for computing, combining, and evaluating conditions inside definitions and queries.

==== Data & Timing Operators (56)

This family provides most of CQL's clinical data-shaping and temporal logic, including list/interval operations, temporal relationships, precision units, and date/time extraction (`difference`, `duration`, parts of dates/times). It is central for measure logic that depends on membership, boundaries, overlap, ordering, and timing windows.

==== Query & Control Flow (21)

This family governs query construction and branching behavior: source and join clauses, filters, projections, sorting/aggregation, and conditional forms (`if/then/else`, `case/when`). These keywords let authors traverse data, shape result sets, and choose outcomes based on evaluated conditions.

=== Types & Literals

==== Type Name (5)

The five built-in data types CQL's operators work with.

Members: <<term-Code,`Code`>>, <<term-Concept,`Concept`>>, <<term-Interval,`Interval`>>, <<term-List,`List`>>, <<term-Tuple,`Tuple`>>

==== Literal (3)

The three fixed values CQL expressions can carry directly.

Members: <<term-false,`false`>>, <<term-null,`null`>>, <<term-true,`true`>>

=== Declarations

==== Declaration (11)

The library-level statements that give a CQL file its shape.

Members: <<term-code,`code`>>, <<term-codesystem,`codesystem`>>, <<term-concept,`concept`>>, <<term-context,`context`>>, <<term-define,`define`>>, <<term-function,`function`>>, <<term-include,`include`>>, <<term-library,`library`>>, <<term-parameter,`parameter`>>, <<term-using,`using`>>, <<term-valueset,`valueset`>>

==== Declaration Modifier (6)

Modifiers that refine a declaration -- an alias, a default, a version.

Members: <<term-called,`called`>>, <<term-codesystems,`codesystems`>>, <<term-default,`default`>>, <<term-display,`display`>>, <<term-fluent,`fluent`>>, <<term-version,`version`>>

==== Access Modifier (2)

Controls whether a definition is visible outside its own library.

Members: <<term-private,`private`>>, <<term-public,`public`>>

=== Core Operators

==== Logical Operator (6)

Boolean connectives, evaluated with three-valued (true/false/null) logic.

Members: <<term-and,`and`>>, <<term-exists,`exists`>>, <<term-implies,`implies`>>, <<term-not,`not`>>, <<term-or,`or`>>, <<term-xor,`xor`>>

==== Arithmetic Operator (6)

Numeric operators beyond the basic +, -, *, /.

Members: <<term-div,`div`>>, <<term-maximum,`maximum`>>, <<term-minimum,`minimum`>>, <<term-mod,`mod`>>, <<term-predecessor,`predecessor`>>, <<term-successor,`successor`>>

==== Type Operator (5)

Casting and type-checking between CQL's types.

Members: <<term-as,`as`>>, <<term-cast,`cast`>>, <<term-convert,`convert`>>, <<term-is,`is`>>, <<term-to,`to`>>

==== Comparison Operator (1)

General-purpose range comparison.

Members: <<term-between,`between`>>

=== Data & Timing Operators

==== List/Interval Operator (17)

Operators for combining, comparing, and inspecting lists and intervals.

Members: <<term-collapse,`collapse`>>, <<term-contains,`contains`>>, <<term-distinct,`distinct`>>, <<term-end,`end`>>, <<term-except,`except`>>, <<term-expand,`expand`>>, <<term-flatten,`flatten`>>, <<term-in,`in`>>, <<term-included-in,`included in`>>, <<term-includes,`includes`>>, <<term-intersect,`intersect`>>, <<term-point,`point`>>, <<term-properly,`properly`>>, <<term-singleton,`singleton`>>, <<term-start,`start`>>, <<term-union,`union`>>, <<term-width,`width`>>

==== Timing Operator (17)

Allen's-interval-algebra-style relationships between dates, times, and intervals.

Members: <<term-after,`after`>>, <<term-before,`before`>>, <<term-during,`during`>>, <<term-ends,`ends`>>, <<term-meets,`meets`>>, <<term-occurs,`occurs`>>, <<term-on,`on`>>, <<term-or-after,`or after`>>, <<term-or-before,`or before`>>, <<term-or-less,`or less`>>, <<term-or-more,`or more`>>, <<term-or-on,`or on`>>, <<term-overlaps,`overlaps`>>, <<term-per,`per`>>, <<term-same,`same`>>, <<term-starts,`starts`>>, <<term-within,`within`>>

==== Time-Precision Unit (16)

The calendar-duration units quantities and date arithmetic are expressed in.

Members: <<term-day,`day`>>, <<term-days,`days`>>, <<term-hour,`hour`>>, <<term-hours,`hours`>>, <<term-millisecond,`millisecond`>>, <<term-milliseconds,`milliseconds`>>, <<term-minute,`minute`>>, <<term-minutes,`minutes`>>, <<term-month,`month`>>, <<term-months,`months`>>, <<term-second,`second`>>, <<term-seconds,`seconds`>>, <<term-week,`week`>>, <<term-weeks,`weeks`>>, <<term-year,`year`>>, <<term-years,`years`>>

==== Extraction Operator (6)

Pulling a component (date, time, precision) out of a date/time value.

Members: <<term-date,`date`>>, <<term-difference,`difference`>>, <<term-duration,`duration`>>, <<term-of,`of`>>, <<term-time,`time`>>, <<term-timezoneoffset,`timezoneoffset`>>

==== Query & Control Flow

===== Query Clause (12)

The clauses that make up a CQL query: source, filter, relate, shape, sort.

Members: <<term-aggregate,`aggregate`>>, <<term-all,`all`>>, <<term-by,`by`>>, <<term-from,`from`>>, <<term-let,`let`>>, <<term-return,`return`>>, <<term-sort,`sort`>>, <<term-starting,`starting`>>, <<term-such-that,`such that`>>, <<term-where,`where`>>, <<term-with,`with`>>, <<term-without,`without`>>

===== Conditional Expression (5)

Branching logic -- choose a result based on a condition.

Members: <<term-case,`case`>>, <<term-else,`else`>>, <<term-if,`if`>>, <<term-then,`then`>>, <<term-when,`when`>>

===== Sort Modifier (4)

Direction keywords for the sort clause.

Members: <<term-asc,`asc`>>, <<term-ascending,`ascending`>>, <<term-desc,`desc`>>, <<term-descending,`descending`>>


.Keyword Dictionary
[%collapsible]
====

All 122 CQL keywords, alphabetically. Definition, example, and current Komet status for each.

[[term-after]]
[discrete]
==== after

`Timing Operator` -- (Data & Timing Operators > Timing Operator) Not yet in Komet

Relational operator testing whether one date, time, or interval occurs later than another (e.g. ‘X after Y’).

[source,cql]
----
"DischargeDateTime" after "AdmissionDateTime"
----



'''

[[term-aggregate]]
[discrete]
==== aggregate

`Query Clause` -- (Query & Control Flow > Query Clause) Not yet in Komet

Introduces the aggregate clause of a query, reducing a list to one accumulated value, similar to a ‘reduce’ function (e.g. ‘aggregate Result starting 0: ...’).

[source,cql]
----
aggregate "RunningTotal" starting 0: RunningTotal + Claim.amount
----



'''

[[term-all]]
[discrete]
==== all

`Query Clause` -- (Query & Control Flow > Query Clause) Not yet in Komet

Modifier that keeps duplicate values instead of the default distinct behavior, used in a return or aggregate clause.

[source,cql]
----
["Condition"] C return all C.code
----



'''

[[term-and]]
[discrete]
==== and

`Logical Operator` -- *Implemented in Komet*

Boolean conjunction — true only if both operands are true (three-valued logic: an unknown operand can yield an unknown result).

[source,cql]
----
AgeInYears() >= 18 and AgeInYears() < 65
----

Komet concept: *And*



'''

[[term-as]]
[discrete]
==== as

`Type Operator` -- (Core Operators > Type Operator) Not yet in Komet

Casts a value to a specified type at compile time (null at run time if the value isn’t that type); also declares a parameter’s or argument’s type.

[source,cql]
----
define function "GetQuantity"(obs Observation): obs.value as Quantity
----



'''

[[term-asc]]
[discrete]
==== asc

`Sort Modifier` -- (Query & Control Flow > Sort Modifier) Not yet in Komet

Short form of ‘ascending’ — orders sort-clause results from lowest to highest (the default order).

[source,cql]
----
["Encounter"] E sort by E.period.start asc
----



'''

[[term-ascending]]
[discrete]
==== ascending

`Sort Modifier` -- (Query & Control Flow > Sort Modifier) Not yet in Komet

Full form of ‘asc’ — orders sort-clause results from lowest to highest.

[source,cql]
----
["Encounter"] E sort by E.period.start ascending
----



'''

[[term-before]]
[discrete]
==== before

`Timing Operator` -- (Data & Timing Operators > Timing Operator) Not yet in Komet

Relational operator testing whether one date, time, or interval occurs earlier than another.

[source,cql]
----
"OnsetDateTime" before "MeasurementPeriod"
----



'''

[[term-between]]
[discrete]
==== between

`Comparison Operator` -- (Core Operators > Comparison Operator) Not yet in Komet

Tests whether a value falls within an inclusive range (e.g. ‘4 between 2 and 8’); also used in ‘duration/difference in <precision> between X and Y’.

[source,cql]
----
4 between 2 and 8
----



'''

[[term-by]]
[discrete]
==== by

`Query Clause` -- (Query & Control Flow > Query Clause) Not yet in Komet

Connector introducing the ordering expression in a sort clause (‘sort by ...’) or the grouping used in an aggregate clause.

[source,cql]
----
["Encounter"] E sort by E.period.start
----



'''

[[term-called]]
[discrete]
==== called

`Declaration Modifier` -- (Declarations > Declaration Modifier) Not yet in Komet

Assigns a local alias to an included library or data model, e.g. ‘include Common called Common’.

[source,cql]
----
include FHIRHelpers version '4.0.1' called FHIRHelpers
----



'''

[[term-case]]
[discrete]
==== case

`Conditional Expression` -- (Query & Control Flow > Conditional Expression) Not yet in Komet

Begins a case expression, which evaluates when/then branches (or compares a value against candidates) and returns the first match.

[source,cql]
----
case
  when "Age" < 18 then 'Pediatric'
  when "Age" < 65 then 'Adult'
  else 'Senior'
end
----



'''

[[term-cast]]
[discrete]
==== cast

`Type Operator` -- (Core Operators > Type Operator) Not yet in Komet

Performs a strict type cast that raises a run-time error if the value isn’t the target type — compare to the more permissive ‘as’.

[source,cql]
----
cast "SomeValue" as Integer
----



'''

[[term-code]]
[discrete]
==== code

`Declaration` -- (Declarations > Declaration) Not yet in Komet

Declares a named, reusable reference to a single terminology code, e.g. ‘code “Foo”: ‘123’ from “SNOMED”’.

[source,cql]
----
code "Diabetes code": '73211009' from "SNOMED"
----



'''

[[term-Code]]
[discrete]
==== Code

`Type Name` -- (Types & Literals > Type Name) Not yet in Komet

Built-in structured type representing one terminology code (elements: code, display, system, version).

[source,cql]
----
define "DiabetesCode": Code '73211009' from "SNOMED" display 'Diabetes mellitus'
----



'''

[[term-codesystem]]
[discrete]
==== codesystem

`Declaration` -- (Declarations > Declaration) Not yet in Komet

Declares a named, local reference to an external code system, e.g. ‘codesystem “SNOMED”: ‘http://snomed.info/sct’’.

[source,cql]
----
codesystem "SNOMED": 'http://snomed.info/sct'
----



'''

[[term-codesystems]]
[discrete]
==== codesystems

`Declaration Modifier` -- (Declarations > Declaration Modifier) Not yet in Komet

Plural form used in a value set’s static binding to pin the specific code systems (and versions) its expansion should use.

[source,cql]
----
valueset "Example": 'urn:oid:2.16.1' codesystems { "SNOMED", "LOINC" }
----



'''

[[term-collapse]]
[discrete]
==== collapse

`List/Interval Operator` -- (Data & Timing Operators > List/Interval Operator) Not yet in Komet

Merges a list of overlapping or adjacent intervals into the smallest possible set of disjoint intervals.

[source,cql]
----
collapse "OverlappingEncounterPeriods"
----



'''

[[term-concept]]
[discrete]
==== concept

`Declaration` -- (Declarations > Declaration) Not yet in Komet

Declares a named, reusable reference to a Concept literal — a set of equivalent codes with a display string.

[source,cql]
----
concept "Diabetes concept": { "Diabetes code" } display 'Diabetes mellitus'
----



'''

[[term-Concept]]
[discrete]
==== Concept

`Type Name` -- (Types & Literals > Type Name) Not yet in Komet

Built-in structured type representing a clinical concept as a list of semantically equivalent Codes plus a display string.

[source,cql]
----
define "DiabetesConcept": Concept { "DiabetesCode" } display 'Diabetes mellitus'
----



'''

[[term-contains]]
[discrete]
==== contains

`List/Interval Operator` -- (Data & Timing Operators > List/Interval Operator) Not yet in Komet

Membership operator testing whether an interval or list contains a given point or element — the inverse of ‘in’.

[source,cql]
----
"MeasurementPeriod" contains "EncounterDateTime"
----



'''

[[term-context]]
[discrete]
==== context

`Declaration` -- (Declarations > Declaration) Not yet in Komet

Declares the evaluation scope for the statements that follow, e.g. Patient, Practitioner, or Unfiltered.

[source,cql]
----
context Patient
----



'''

[[term-convert]]
[discrete]
==== convert

`Type Operator` -- (Core Operators > Type Operator) Not yet in Komet

Explicitly converts a value or quantity unit from one type/unit to another, e.g. ‘convert X to DateTime’.

[source,cql]
----
convert 5000 'g' to 'kg'
----



'''

[[term-date]]
[discrete]
==== date

`Extraction Operator` -- (Data & Timing Operators > Extraction Operator) Not yet in Komet

Extracts the date-only portion of a DateTime value: ‘date from X’.

[source,cql]
----
date from "EncounterDateTime"
----



'''

[[term-day]]
[discrete]
==== day

`Time-Precision Unit` -- (Data & Timing Operators > Time-Precision Unit) Not yet in Komet

Singular calendar-duration unit of one day, used in quantities and date/time arithmetic.

[source,cql]
----
AdmissionDateTime + 1 day
----



'''

[[term-days]]
[discrete]
==== days

`Time-Precision Unit` -- (Data & Timing Operators > Time-Precision Unit) Not yet in Komet

Plural calendar-duration unit of days.

[source,cql]
----
Interval["AdmissionDateTime", "AdmissionDateTime" + 3 days]
----



'''

[[term-default]]
[discrete]
==== default

`Declaration Modifier` -- (Declarations > Declaration Modifier) Not yet in Komet

Specifies a compile-time default value for a parameter, used when the evaluating environment supplies none.

[source,cql]
----
parameter "MeasurementPeriod" Interval<DateTime>
  default Interval[@2024-01-01T00:00:00.0, @2025-01-01T00:00:00.0)
----



'''

[[term-define]]
[discrete]
==== define

`Declaration` -- (Declarations > Declaration) Not yet in Komet

Introduces a named expression — the basic unit of logic in a CQL library.

[source,cql]
----
define "InInitialPopulation": exists "QualifyingEncounters"
----



'''

[[term-desc]]
[discrete]
==== desc

`Sort Modifier` -- (Query & Control Flow > Sort Modifier) Not yet in Komet

Short form of ‘descending’ — orders sort-clause results from highest to lowest.

[source,cql]
----
["Encounter"] E sort by E.period.start desc
----



'''

[[term-descending]]
[discrete]
==== descending

`Sort Modifier` -- (Query & Control Flow > Sort Modifier) Not yet in Komet

Full form of ‘desc’ — orders sort-clause results from highest to lowest.

[source,cql]
----
["Encounter"] E sort by E.period.start descending
----



'''

[[term-difference]]
[discrete]
==== difference

`Extraction Operator` -- (Data & Timing Operators > Extraction Operator) Not yet in Komet

Counts the number of precision boundaries crossed between two date/time values: ‘difference in months between X and Y’.

[source,cql]
----
difference in days between "AdmissionDateTime" and "DischargeDateTime"
----



'''

[[term-display]]
[discrete]
==== display

`Declaration Modifier` -- (Declarations > Declaration Modifier) Not yet in Komet

Supplies the human-readable display string on a Code or Concept declaration or literal.

[source,cql]
----
code "Diabetes code": '73211009' from "SNOMED" display 'Diabetes mellitus'
----



'''

[[term-distinct]]
[discrete]
==== distinct

`List/Interval Operator` -- (Data & Timing Operators > List/Interval Operator) Not yet in Komet

Removes duplicate elements from a list; also the default (implicit) behavior of a query’s return clause.

[source,cql]
----
distinct { 1, 1, 2, 3 }
----



'''

[[term-div]]
[discrete]
==== div

`Arithmetic Operator` -- (Core Operators > Arithmetic Operator) Not yet in Komet

Truncated (integer-style) division, discarding any remainder — e.g. ‘4 div 2’ = 2.

[source,cql]
----
10 div 3
----



'''

[[term-duration]]
[discrete]
==== duration

`Extraction Operator` -- (Data & Timing Operators > Extraction Operator) Not yet in Komet

Computes the number of whole calendar periods of a given precision between two date/time values: ‘duration in months between X and Y’.

[source,cql]
----
duration in days between "AdmissionDateTime" and "DischargeDateTime"
----



'''

[[term-during]]
[discrete]
==== during

`Timing Operator` -- (Data & Timing Operators > Timing Operator) Not yet in Komet

Tests whether a point or interval falls entirely within another interval.

[source,cql]
----
"EncounterDateTime" during "MeasurementPeriod"
----



'''

[[term-else]]
[discrete]
==== else

`Conditional Expression` -- (Query & Control Flow > Conditional Expression) Not yet in Komet

Introduces the fallback branch of an if or case expression, used when no condition evaluates to true.

[source,cql]
----
if AgeInYears() >= 18 then 'Adult' else 'Minor'
----



'''

[[term-end]]
[discrete]
==== end

`List/Interval Operator` -- (Data & Timing Operators > List/Interval Operator) Not yet in Komet

Returns the ending boundary point of an interval: ‘end of Interval[3, 5)’.

[source,cql]
----
end of "MeasurementPeriod"
----



'''

[[term-ends]]
[discrete]
==== ends

`Timing Operator` -- (Data & Timing Operators > Timing Operator) Not yet in Komet

Tests whether one interval finishes at the same point as another (a ‘suffix’ relationship, from Allen’s interval algebra).

[source,cql]
----
"EncounterAPeriod" ends "EncounterBPeriod"
----



'''

[[term-except]]
[discrete]
==== except

`List/Interval Operator` -- (Data & Timing Operators > List/Interval Operator) Not yet in Komet

Set-difference operator — returns the elements or range in the left operand that are not in the right operand.

[source,cql]
----
"AllPatients" except "ExcludedPatients"
----



'''

[[term-exists]]
[discrete]
==== exists

`Logical Operator` -- (Core Operators > Logical Operator) Not yet in Komet

Existential quantifier that returns true if a list or query has at least one non-null element.

[source,cql]
----
exists "QualifyingEncounters"
----



'''

[[term-expand]]
[discrete]
==== expand

`List/Interval Operator` -- (Data & Timing Operators > List/Interval Operator) Not yet in Komet

Expands an interval into a list of unit-length sub-intervals at a given precision, or expands a value set reference into its member codes.

[source,cql]
----
expand "MeasurementPeriod" per day
----



'''

[[term-false]]
[discrete]
==== false

`Literal` -- (Types & Literals > Literal) Not yet in Komet

The boolean literal representing logical falsity.

[source,cql]
----
define "IsExcluded": false
----



'''

[[term-flatten]]
[discrete]
==== flatten

`List/Interval Operator` -- (Data & Timing Operators > List/Interval Operator) Not yet in Komet

Flattens a list of lists into a single, single-level list.

[source,cql]
----
flatten { { 1, 2 }, { 3, 4 } }
----



'''

[[term-fluent]]
[discrete]
==== fluent

`Declaration Modifier` -- (Declarations > Declaration Modifier) Not yet in Komet

Marks a function so it can be invoked with dot-notation, with the left-hand value supplied as its first argument.

[source,cql]
----
define fluent function "ToAgeInYears"(BirthDate Date): AgeInYears(BirthDate)
----



'''

[[term-from]]
[discrete]
==== from

`Query Clause` -- (Query & Control Flow > Query Clause) Not yet in Komet

Introduces the source of a query, a code declaration’s originating code system, or an extraction operator (e.g. ‘date from X’, ‘singleton from X’).

[source,cql]
----
from [Encounter] E, [Condition] C where E.subject = C.subject
----



'''

[[term-function]]
[discrete]
==== function

`Declaration` -- (Declarations > Declaration) Not yet in Komet

Declares a named, reusable function that accepts typed arguments and returns a value.

[source,cql]
----
define function "CalculateAge"(BirthDate Date): AgeInYears(BirthDate)
----



'''

[[term-hour]]
[discrete]
==== hour

`Time-Precision Unit` -- (Data & Timing Operators > Time-Precision Unit) Not yet in Komet

Singular calendar-duration unit of one hour.

[source,cql]
----
"AdmissionDateTime" + 1 hour
----



'''

[[term-hours]]
[discrete]
==== hours

`Time-Precision Unit` -- (Data & Timing Operators > Time-Precision Unit) Not yet in Komet

Plural calendar-duration unit of hours.

[source,cql]
----
"AdmissionDateTime" + 24 hours
----



'''

[[term-if]]
[discrete]
==== if

`Conditional Expression` -- (Query & Control Flow > Conditional Expression) Not yet in Komet

Begins an if/then/else expression that selects between two results based on one boolean condition.

[source,cql]
----
if AgeInYears() >= 18 then 'Adult' else 'Minor'
----



'''

[[term-implies]]
[discrete]
==== implies

`Logical Operator` -- (Core Operators > Logical Operator) Not yet in Komet

Logical implication — false only when the left operand is true and the right is false.

[source,cql]
----
"HasDiagnosis" implies "InInitialPopulation"
----



'''

[[term-in]]
[discrete]
==== in

`List/Interval Operator` -- (Data & Timing Operators > List/Interval Operator) Not yet in Komet

Tests whether a value is a member of a list, interval, or terminology set — the inverse of ‘contains’.

[source,cql]
----
"EncounterDateTime" in "MeasurementPeriod"
----



'''

[[term-include]]
[discrete]
==== include

`Declaration` -- (Declarations > Declaration) Not yet in Komet

Adds a reference to another CQL library so its public components can be used here.

[source,cql]
----
include FHIRHelpers version '4.0.1' called FHIRHelpers
----



'''

[[term-included-in]]
[discrete]
==== included in

`List/Interval Operator` -- _Related concept in Komet_

Tests whether one interval or list is entirely contained within another — the inverse of ‘includes’.

[source,cql]
----
"EncounterPeriod" included in "MeasurementPeriod"
----

Related Komet concept(s): *Clause Included in*



'''

[[term-includes]]
[discrete]
==== includes

`List/Interval Operator` -- (Data & Timing Operators > List/Interval Operator) Not yet in Komet

Tests whether one interval or list entirely contains another — the inverse of ‘included in’.

[source,cql]
----
"MeasurementPeriod" includes "EncounterPeriod"
----



'''

[[term-intersect]]
[discrete]
==== intersect

`List/Interval Operator` -- (Data & Timing Operators > List/Interval Operator) Not yet in Komet

Returns the elements, or overlapping range, common to both operands.

[source,cql]
----
"MeasurementPeriod" intersect "TreatmentPeriod"
----



'''

[[term-Interval]]
[discrete]
==== Interval

`Type Name` -- _Related concept in Komet_

Built-in type representing a range of ordered values, e.g. ‘Interval[3, 5)’.

[source,cql]
----
define "MeasurementPeriod": Interval[@2024-01-01, @2024-12-31]
----

Related Komet concept(s): *Interval Set Axioms*, *Interval role*, *Interval Upper Bound*, *Interval Lower Bound*, *Include Upper Bound*



'''

[[term-is]]
[discrete]
==== is

`Type Operator` -- (Core Operators > Type Operator) Not yet in Komet

Tests the run-time type of a value (e.g. ‘5 is Integer’), or tests for null/true/false (e.g. ‘X is null’).

[source,cql]
----
"Value" is Integer
----



'''

[[term-let]]
[discrete]
==== let

`Query Clause` -- (Query & Control Flow > Query Clause) Not yet in Komet

Defines a named, reusable intermediate expression scoped to a single query.

[source,cql]
----
["Encounter"] E let EncounterStart: E.period.start return EncounterStart
----



'''

[[term-library]]
[discrete]
==== library

`Declaration` -- (Declarations > Declaration) Not yet in Komet

Names the current CQL library and, optionally, its version.

[source,cql]
----
library CQMExample version '1.0.0'
----



'''

[[term-List]]
[discrete]
==== List

`Type Name` -- (Types & Literals > Type Name) Not yet in Komet

Built-in type representing an ordered, zero-indexed collection of values of the same type.

[source,cql]
----
define "Encounters": [Encounter] E return E.period
----



'''

[[term-maximum]]
[discrete]
==== maximum

`Arithmetic Operator` -- (Core Operators > Arithmetic Operator) Not yet in Komet

Returns the maximum representable value for a given type, e.g. ‘maximum Integer’.

[source,cql]
----
maximum Integer
----



'''

[[term-meets]]
[discrete]
==== meets

`Timing Operator` -- (Data & Timing Operators > Timing Operator) Not yet in Komet

Tests whether two intervals are adjacent — one ends exactly where the other begins.

[source,cql]
----
"Period1" meets "Period2"
----



'''

[[term-millisecond]]
[discrete]
==== millisecond

`Time-Precision Unit` -- (Data & Timing Operators > Time-Precision Unit) Not yet in Komet

Singular calendar-duration unit of one millisecond — the finest date/time precision.

[source,cql]
----
"Timestamp" + 1 millisecond
----



'''

[[term-milliseconds]]
[discrete]
==== milliseconds

`Time-Precision Unit` -- (Data & Timing Operators > Time-Precision Unit) Not yet in Komet

Plural calendar-duration unit of milliseconds.

[source,cql]
----
duration in milliseconds between "Start" and "End"
----



'''

[[term-minimum]]
[discrete]
==== minimum

`Arithmetic Operator` -- (Core Operators > Arithmetic Operator) Not yet in Komet

Returns the minimum representable value for a given type, e.g. ‘minimum Integer’.

[source,cql]
----
minimum Integer
----



'''

[[term-minute]]
[discrete]
==== minute

`Time-Precision Unit` -- (Data & Timing Operators > Time-Precision Unit) Not yet in Komet

Singular calendar-duration unit of one minute.

[source,cql]
----
"AdmissionDateTime" + 1 minute
----



'''

[[term-minutes]]
[discrete]
==== minutes

`Time-Precision Unit` -- (Data & Timing Operators > Time-Precision Unit) Not yet in Komet

Plural calendar-duration unit of minutes.

[source,cql]
----
duration in minutes between "Start" and "End"
----



'''

[[term-mod]]
[discrete]
==== mod

`Arithmetic Operator` -- (Core Operators > Arithmetic Operator) Not yet in Komet

Modulo operator — returns the remainder of dividing the left operand by the right.

[source,cql]
----
10 mod 3
----



'''

[[term-month]]
[discrete]
==== month

`Time-Precision Unit` -- (Data & Timing Operators > Time-Precision Unit) Not yet in Komet

Singular calendar-duration unit of one month.

[source,cql]
----
"AdmissionDateTime" + 1 month
----



'''

[[term-months]]
[discrete]
==== months

`Time-Precision Unit` -- (Data & Timing Operators > Time-Precision Unit) Not yet in Komet

Plural calendar-duration unit of months.

[source,cql]
----
duration in months between "Start" and "End"
----



'''

[[term-not]]
[discrete]
==== not

`Logical Operator` -- (Core Operators > Logical Operator) Not yet in Komet

Logical negation — true becomes false, false becomes true, null stays null.

[source,cql]
----
not exists "Exclusions"
----



'''

[[term-null]]
[discrete]
==== null

`Literal` -- (Types & Literals > Literal) Not yet in Komet

The literal representing an unknown or missing value.

[source,cql]
----
"BirthDate" is null
----



'''

[[term-occurs]]
[discrete]
==== occurs

`Timing Operator` -- (Data & Timing Operators > Timing Operator) Not yet in Komet

Optional word that may appear anywhere ‘starts’ or ‘ends’ can appear in a timing phrase, purely to make it read more naturally.

[source,cql]
----
"Condition" occurs during "MeasurementPeriod"
----



'''

[[term-of]]
[discrete]
==== of

`Extraction Operator` -- (Data & Timing Operators > Extraction Operator) Not yet in Komet

Connector used in boundary-extraction phrases (‘start of X’, ‘end of X’) and qualified date comparisons (‘after year of X’).

[source,cql]
----
start of "MeasurementPeriod"
----



'''

[[term-on]]
[discrete]
==== on

`Timing Operator` -- (Data & Timing Operators > Timing Operator) Not yet in Komet

Timing keyword combined with ‘or before’ / ‘or after’ to build the inclusive-boundary On Or Before / On Or After operators.

[source,cql]
----
"DischargeDateTime" on or before end of "MeasurementPeriod"
----



'''

[[term-or]]
[discrete]
==== or

`Logical Operator` -- *Implemented in Komet*

Boolean inclusive disjunction — true if either or both operands are true.

[source,cql]
----
"HasCondition" or "HasMedication"
----

Komet concept: *Or*



'''

[[term-or-after]]
[discrete]
==== or after

`Timing Operator` -- (Data & Timing Operators > Timing Operator) Not yet in Komet

Qualifies a precision- or quantity-based timing comparison to include the boundary case in the ‘after’ direction, e.g. ‘same year or after’, ‘3 days or after’.

[source,cql]
----
"DischargeDateTime" 3 days or after "AdmissionDateTime"
----



'''

[[term-or-before]]
[discrete]
==== or before

`Timing Operator` -- (Data & Timing Operators > Timing Operator) Not yet in Komet

Qualifies a precision- or quantity-based timing comparison to include the boundary case in the ‘before’ direction, e.g. ‘same year or before’.

[source,cql]
----
"FollowUpDateTime" 30 days or before "SurgeryDateTime"
----



'''

[[term-or-less]]
[discrete]
==== or less

`Timing Operator` -- _Related concept in Komet_

Qualifies a quantity offset in a timing phrase to mean ‘that amount or a smaller amount’, e.g. ‘occurring 3 days or less before...’.

[source,cql]
----
"FollowUpDateTime" 3 days or less after "DischargeDateTime"
----

Related Komet concept(s): *Clause Less or equal*



'''

[[term-or-more]]
[discrete]
==== or more

`Timing Operator` -- _Related concept in Komet_

Qualifies a quantity offset in a timing phrase to mean ‘that amount or a larger amount’.

[source,cql]
----
"ReadmissionDateTime" 30 days or more after "DischargeDateTime"
----

Related Komet concept(s): *Clause Greater or equal*



'''

[[term-or-on]]
[discrete]
==== or on

`Timing Operator` -- (Data & Timing Operators > Timing Operator) Not yet in Komet

Qualifies a timing comparison to include the exact-boundary case, used to build the On Or Before / On Or After operators.

[source,cql]
----
"DischargeDateTime" before or on end of "MeasurementPeriod"
----



'''

[[term-overlaps]]
[discrete]
==== overlaps

`Timing Operator` -- _Related concept in Komet_

Tests whether two intervals share at least one point in common.

[source,cql]
----
"Period1" overlaps "Period2"
----

Related Komet concept(s): *Clause Overlaps*



'''

[[term-parameter]]
[discrete]
==== parameter

`Declaration` -- (Declarations > Declaration) Not yet in Komet

Declares a named, typed input the evaluating environment supplies to the library at run time.

[source,cql]
----
parameter "MeasurementPeriod" Interval<DateTime>
----



'''

[[term-per]]
[discrete]
==== per

`Timing Operator` -- (Data & Timing Operators > Timing Operator) Not yet in Komet

Reserved keyword associated with rate- or frequency-style expressions.

[source,cql]
----
expand "MeasurementPeriod" per day
----



'''

[[term-point]]
[discrete]
==== point

`List/Interval Operator` -- (Data & Timing Operators > List/Interval Operator) Not yet in Komet

Extracts the single value from a unit interval — an interval whose start and end are the same point: ‘point from Interval[1,1]’.

[source,cql]
----
point from Interval[5, 5]
----



'''

[[term-predecessor]]
[discrete]
==== predecessor

`Arithmetic Operator` -- (Core Operators > Arithmetic Operator) Not yet in Komet

Returns the value immediately preceding the argument for its type, e.g. ‘predecessor of 100’ = 99 (steps back by the value’s finest specified precision for Date/DateTime/Time).

[source,cql]
----
predecessor of 5
----



'''

[[term-private]]
[discrete]
==== private

`Access Modifier` -- (Declarations > Access Modifier) Not yet in Komet

Marks a library component as accessible only within the library where it’s declared.

[source,cql]
----
define private "HelperCalculation": 1 + 1
----



'''

[[term-properly]]
[discrete]
==== properly

`List/Interval Operator` -- (Data & Timing Operators > List/Interval Operator) Not yet in Komet

Strengthens ‘includes’ / ‘included in’ so an exactly-equal value no longer counts — i.e., a true proper superset/subset.

[source,cql]
----
"MeasurementPeriod" properly includes "EncounterPeriod"
----



'''

[[term-public]]
[discrete]
==== public

`Access Modifier` -- (Declarations > Access Modifier) Not yet in Komet

Marks a library component as accessible to other libraries that include this one — the default when no modifier is given.

[source,cql]
----
define public "InitialPopulation": true
----



'''

[[term-return]]
[discrete]
==== return

`Query Clause` -- (Query & Control Flow > Query Clause) Not yet in Komet

Shapes or projects a query’s result, optionally computing new values; defaults to distinct results unless ‘all’ is specified.

[source,cql]
----
["Condition"] C return C.code
----



'''

[[term-same]]
[discrete]
==== same

`Timing Operator` -- (Data & Timing Operators > Timing Operator) Not yet in Komet

Precision-based equality comparison for date/time values: ‘same as’, ‘same year as’, etc.

[source,cql]
----
"AdmissionDateTime" same day as "OnsetDateTime"
----



'''

[[term-second]]
[discrete]
==== second

`Time-Precision Unit` -- (Data & Timing Operators > Time-Precision Unit) Not yet in Komet

Singular calendar-duration unit of one second.

[source,cql]
----
"AdmissionDateTime" + 1 second
----



'''

[[term-seconds]]
[discrete]
==== seconds

`Time-Precision Unit` -- (Data & Timing Operators > Time-Precision Unit) Not yet in Komet

Plural calendar-duration unit of seconds.

[source,cql]
----
duration in seconds between "Start" and "End"
----



'''

[[term-singleton]]
[discrete]
==== singleton

`List/Interval Operator` -- (Data & Timing Operators > List/Interval Operator) Not yet in Komet

Extracts the single element from a one-element list: ‘singleton from X’ — used for list-to-scalar demotion.

[source,cql]
----
singleton from ["Patient"]
----



'''

[[term-sort]]
[discrete]
==== sort

`Query Clause` -- (Query & Control Flow > Query Clause) Not yet in Komet

Introduces the sort clause that orders a query’s results; must be the last clause in a query.

[source,cql]
----
["Encounter"] E sort by E.period.start
----



'''

[[term-start]]
[discrete]
==== start

`List/Interval Operator` -- (Data & Timing Operators > List/Interval Operator) Not yet in Komet

Returns the starting boundary point of an interval: ‘start of Interval[3, 5)’.

[source,cql]
----
start of "MeasurementPeriod"
----



'''

[[term-starting]]
[discrete]
==== starting

`Query Clause` -- (Query & Control Flow > Query Clause) Not yet in Komet

Supplies the initial accumulator value for an aggregate clause, e.g. ‘aggregate Result starting 0: ...’.

[source,cql]
----
aggregate "RunningTotal" starting 0: RunningTotal + Claim.amount
----



'''

[[term-starts]]
[discrete]
==== starts

`Timing Operator` -- (Data & Timing Operators > Timing Operator) Not yet in Komet

Tests whether one interval begins at the same point as another (a ‘prefix’ relationship, from Allen’s interval algebra).

[source,cql]
----
"Period1" starts "Period2"
----



'''

[[term-successor]]
[discrete]
==== successor

`Arithmetic Operator` -- (Core Operators > Arithmetic Operator) Not yet in Komet

Returns the value immediately following the argument for its type, e.g. ‘successor of 1’ = 2.

[source,cql]
----
successor of 5
----



'''

[[term-such-that]]
[discrete]
==== such that

`Query Clause` -- (Query & Control Flow > Query Clause) Not yet in Komet

Attaches the filtering condition to a with/without relationship clause.

[source,cql]
----
["Encounter"] E with ["Condition"] C such that C.onset during E.period
----



'''

[[term-then]]
[discrete]
==== then

`Conditional Expression` -- (Query & Control Flow > Conditional Expression) Not yet in Komet

Introduces the result returned when the preceding if/when condition evaluates to true.

[source,cql]
----
if AgeInYears() >= 18 then 'Adult' else 'Minor'
----



'''

[[term-time]]
[discrete]
==== time

`Extraction Operator` -- (Data & Timing Operators > Extraction Operator) Not yet in Komet

Extracts the time-of-day portion of a DateTime value: ‘time from X’.

[source,cql]
----
time from "EncounterDateTime"
----



'''

[[term-timezoneoffset]]
[discrete]
==== timezoneoffset

`Extraction Operator` -- (Data & Timing Operators > Extraction Operator) Not yet in Komet

Extracts the timezone-offset component of a Date/DateTime/Time value: ‘timezoneoffset from X’.

[source,cql]
----
timezoneoffset from "EncounterDateTime"
----



'''

[[term-to]]
[discrete]
==== to

`Type Operator` -- (Core Operators > Type Operator) Not yet in Komet

Connector used with ‘convert’ to name the target type or unit: ‘convert X to Y’.

[source,cql]
----
convert 98.6 '[degF]' to 'Cel'
----



'''

[[term-true]]
[discrete]
==== true

`Literal` -- (Types & Literals > Literal) Not yet in Komet

The boolean literal representing logical truth.

[source,cql]
----
define "MeetsInitialPopulation": true
----



'''

[[term-Tuple]]
[discrete]
==== Tuple

`Type Name` -- (Types & Literals > Type Name) Not yet in Komet

Built-in type representing a structured value made of named elements, each with its own type.

[source,cql]
----
define "PatientSummary": Tuple { name: 'Jane Doe', age: 45 }
----



'''

[[term-union]]
[discrete]
==== union

`List/Interval Operator` -- (Data & Timing Operators > List/Interval Operator) Not yet in Komet

Combines two lists or intervals into one (duplicates removed for lists; for intervals, only defined when they overlap or meet).

[source,cql]
----
"Group1" union "Group2"
----



'''

[[term-using]]
[discrete]
==== using

`Declaration` -- (Declarations > Declaration) Not yet in Komet

Declares which external data model (e.g. FHIR, QUICK) the library’s retrieve expressions are written against.

[source,cql]
----
using FHIR version '4.0.1'
----



'''

[[term-valueset]]
[discrete]
==== valueset

`Declaration` -- (Declarations > Declaration) Not yet in Komet

Declares a named, local reference to an external value set.

[source,cql]
----
valueset "Diabetes": 'urn:oid:2.16.840.1.113883.3.464.1003.103.12.1001'
----



'''

[[term-version]]
[discrete]
==== version

`Declaration Modifier` -- (Declarations > Declaration Modifier) Not yet in Komet

Specifies a version identifier on a library, codesystem, valueset, or include declaration.

[source,cql]
----
library CQMExample version '1.0.0'
----



'''

[[term-week]]
[discrete]
==== week

`Time-Precision Unit` -- (Data & Timing Operators > Time-Precision Unit) Not yet in Komet

Singular calendar-duration unit of one week (always treated as 7 days).

[source,cql]
----
"AdmissionDateTime" + 1 week
----



'''

[[term-weeks]]
[discrete]
==== weeks

`Time-Precision Unit` -- (Data & Timing Operators > Time-Precision Unit) Not yet in Komet

Plural calendar-duration unit of weeks.

[source,cql]
----
duration in weeks between "Start" and "End"
----



'''

[[term-when]]
[discrete]
==== when

`Conditional Expression` -- (Query & Control Flow > Conditional Expression) Not yet in Komet

Introduces a condition (or a candidate value) inside a case expression.

[source,cql]
----
case
  when "Age" < 18 then 'Pediatric'
  else 'Adult'
end
----



'''

[[term-where]]
[discrete]
==== where

`Query Clause` -- (Query & Control Flow > Query Clause) Not yet in Komet

Filters a query’s results to only the elements for which a boolean condition evaluates to true.

[source,cql]
----
["Condition"] C where C.clinicalStatus = 'active'
----



'''

[[term-width]]
[discrete]
==== width

`List/Interval Operator` -- (Data & Timing Operators > List/Interval Operator) Not yet in Komet

Returns the size of an interval — its high boundary minus its low boundary.

[source,cql]
----
width of "MeasurementPeriod"
----



'''

[[term-with]]
[discrete]
==== with

`Query Clause` -- (Query & Control Flow > Query Clause) Not yet in Komet

Filters a query’s primary source to elements that have a specified relationship to another source, paired with ‘such that’.

[source,cql]
----
["Encounter"] E with ["Condition"] C such that C.encounter = E.id
----



'''

[[term-within]]
[discrete]
==== within

`Timing Operator` -- (Data & Timing Operators > Timing Operator) Not yet in Komet

Tests whether a point or interval falls within a specified proximity or range of another.

[source,cql]
----
"OnsetDateTime" within 7 days of "AdmissionDateTime"
----



'''

[[term-without]]
[discrete]
==== without

`Query Clause` -- (Query & Control Flow > Query Clause) Not yet in Komet

Filters a query’s primary source to elements that do NOT have a specified relationship to another source — the negated form of ‘with’.

[source,cql]
----
["Encounter"] E without ["Condition"] C such that C.encounter = E.id
----



'''

[[term-xor]]
[discrete]
==== xor

`Logical Operator` -- (Core Operators > Logical Operator) Not yet in Komet

Boolean exclusive-or — true only when exactly one operand is true.

[source,cql]
----
"IsInpatient" xor "IsOutpatient"
----



'''

[[term-year]]
[discrete]
==== year

`Time-Precision Unit` -- (Data & Timing Operators > Time-Precision Unit) Not yet in Komet

Singular calendar-duration unit of one year.

[source,cql]
----
"AdmissionDateTime" + 1 year
----



'''

[[term-years]]
[discrete]
==== years

`Time-Precision Unit` -- (Data & Timing Operators > Time-Precision Unit) Not yet in Komet

Plural calendar-duration unit of years.

[source,cql]
----
AgeInYears() >= 30
----
====


== Tinkar Base Model

This chapter describes the component/chronicle/pattern-of-patterns meta-model every
other chapter's own terminology ultimately descends from.

=== Components and Chronicles

koncept-narrative::TinkarModelConcept[]

The base-model shape patterns' own tables follow, in the order the narrative names
them: the chronicle and version shapes for components, concepts, patterns,
semantics, and STAMPs, then the base-model membership patterns.

koncept-pattern-table::ComponentChronologyPattern[]

koncept-pattern-table::ComponentVersionPattern[]

koncept-pattern-table::ConceptFieldPattern[]

koncept-pattern-table::ConceptVersionPattern[]

koncept-pattern-table::PatternChronologyPattern[]

koncept-pattern-table::PatternVersionPattern[]

koncept-pattern-table::SemanticChronologyPattern[]

koncept-pattern-table::SemanticVersionFieldPattern[]

koncept-pattern-table::STAMPChronologyPattern[]

koncept-pattern-table::STAMPVersionFieldPattern[]

koncept-pattern-table::TinkarBaseModelComponentPattern[]

koncept-pattern-table::KometBaseModelComponentPattern[]

=== Base Model Primitives

koncept-narrative::Object[]

== Value Constraints and Reference Ranges

koncept-narrative::ValueConstraint[]

<<koncept-pattern-table-ValueConstraintPattern>> shows the pattern's shape.

koncept-pattern-table::ValueConstraintPattern[]

== Authoring Actions and Legacy Constraints

koncept-narrative::ActionProperties[]

== Feature-Based and Intrinsic Roles

koncept-narrative::DescriptionType[]

== Annotations, Correlations and Editor Focus

koncept-narrative::AnnotationType[]

<<koncept-pattern-table-CommentPattern>>,
<<koncept-pattern-table-StarterSetAuthorRosterPattern>>, and
<<koncept-pattern-table-PreferredReviewerPattern>> show these three patterns'
shapes.

koncept-pattern-table::CommentPattern[]

koncept-pattern-table::StarterSetAuthorRosterPattern[]

koncept-pattern-table::PreferredReviewerPattern[]

== Identifiers

koncept-narrative::IdentifierSource[]

<<koncept-pattern-table-IdentifierPattern>> shows the identifier pattern's shape.

koncept-pattern-table::IdentifierPattern[]

== Coordinate Properties: Language and Path

koncept-narrative::LanguageCoordinateProperties[]

== Tree Amalgams

koncept-narrative::TreeAmalgamProperties[]

== Clinical Phenomena and Domain Content

koncept-narrative::Phenomenon[]

== Object Properties and Relationships

koncept-narrative::ObjectProperties[]

== Provenance and Root

koncept-narrative::IntegratedKnowledgeManagement[]

// The comprehensive, alphabetical Koncept Glossary is appended automatically
// (koncept-glossary-all) as the final chapter.
